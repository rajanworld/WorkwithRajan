{
  "version": 2,
  "framework": "vite",
  "buildCommand": "npm install && NODE_ENV=production npm run vercel-build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "env": {
    "NODE_ENV": "production"
  }
}
